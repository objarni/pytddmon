language: python

python:
  - '2.6'
  - '2.7'
  - '3.2'
  - '3.3'

before_install:
  - sudo apt-get update

script:
  - nosetests
  - touch /tmp/tmpstuff.txt
  - ( cd systest && python ./systest.py )
